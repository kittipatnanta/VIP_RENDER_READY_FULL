
VIP RENDER READY – FULL SYSTEM

Direct ZIP upload to Render.

Includes:
- Admin Dashboard (Login)
- Click → Signup → Revenue tracking
- Redirect /go/{id}
- Payment webhook
- Render-ready (Procfile)

Run:
pip install -r requirements.txt
python app.py
